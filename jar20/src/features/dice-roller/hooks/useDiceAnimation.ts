/**
 * Phase 4 hook.
 * Will drive the 3D roll animation via Framer Motion / CSS transforms.
 * Stub only — returns a no-op until Phase 4 is implemented.
 */
export function useDiceAnimation() {
  const triggerRollAnimation = async (): Promise<void> => {
    // TODO Phase 4: animate dice rolling across screen, handle collisions
    return Promise.resolve()
  }

  return { triggerRollAnimation }
}
